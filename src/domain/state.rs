use crate::domain::domain_id::DomainId;

pub struct DomainState {
    pub id: DomainId,
    // internal mutable fields
}

// #[allow(dead_code)]
impl DomainState {
    // DELETE apply_mutation completely
}
