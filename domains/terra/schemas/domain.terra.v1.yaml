schema: domain
id: domain.terra
version: v1.0
parent: domain.lifepush
description: >
  The realm of embodiment and consequence.
  Terra encompasses all physical substrates—organic or synthetic—
  where LifePush expresses as measurable, interactive systems.
fields:
  - name: medium
    type: string
    enum: [biological, synthetic, hybrid]
  - name: instance_ref
    type: string
    description: Reference to the specific material entity.
notes: >
  Terra holds human bodies, animal organisms, and AI hardware alike.
  It is the layer where choices have real-world impact.
