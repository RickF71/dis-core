# Bootstrap Removal Rules
# Copilot MUST NOT Generate

Copilot must NOT reintroduce any of the following:

❌ PhaseS0PrimeSeatSetup
❌ BootstrapIdentityTriads (GOV-1)
❌ RegisterSyntheticDomains (GOV-6)
❌ terra/numen/lima auto-seeding
❌ auto.corporeal global generation
❌ testmech global generation
❌ global identity triad binding
❌ universal seat sweeps
❌ synthetic domain factories
❌ default domain stacks created at boot

Bootstrap must be:

- Minimal
- Non-authoritarian
- Only domain.null is created
- Everything else loads via Universal Domain Loader
