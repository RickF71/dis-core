allowed:
  domain.null:
    - identity.confirm
    - identity.update
  domain.terra:
    - land.register
    - land.transfer
    - verify       # ✅ add this
  domain.simula-terra:
    - identity.confirm
deny:
  - domain.ghost
