# Demo policy for v0.5 — Policy-Driven Sovereignty
allowed:
  domain.null:
    - identity.confirm
    - identity.update
  domain.terra:
    - land.register
    - land.transfer
  domain.simula-terra:
    - identity.confirm
deny:
  - domain.ghost
