# DIS-Core Documentation

Developer tools:

- Run `tools/dimension_audit.sh` to find legacy null → terra assumptions and dimension offsets before making structural changes to the spine.

Other docs live alongside feature-specific files in this folder.
