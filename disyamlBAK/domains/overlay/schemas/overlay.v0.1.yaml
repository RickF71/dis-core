$schema: "https://dis.core/domains/dis/schemas/overlay.v0.1"
title: "Overlay Envelope (v0.1)"
type: object
required: ["$schema","domain","scope","version","data"]
properties:
  $schema: { type: string, const: "overlay.v0.1" }
  domain: { type: string }
  scope: { type: string, enum: ["authority","trust","consent","freeze"] }
  version: { type: string }
  data:
    type: object
    description: "Overlay payload (graph/table/metrics)."
    additionalProperties: true
